---
trigger: always_on
---

Always check if there is a folder with experiment prepended in the name at the root of the project, if yes, modify the content of the experiment instead of the same files at the root